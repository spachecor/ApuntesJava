package temados.poo.abstraer;

public class Transporte {
	int velocidadPunta, media;
	long kilometraje;
	String marca, modelo, tipoMotor, tipoTransporte;
	
	/*ABSTRAER es la capacidad que permide distinguir aquellas características fundamentales de un
	 * objeto que lo hacen diferente al resto, y que proporcionan límites conceptuales bien definidos
	 * relativos a la perspectiva del que lo visualiza.
	 * Una abstracción se focaliza sobre una posible vista, ayudando a separar e comportamiento
	 * esencial de un objeto de tu implementación.
	 * Los lenguajes de poo facilitan abstraer gracias a que permiten definir interfaces comunes
	 * para comunicarse con clases de objetos. Estas interfaces están compuestas por los métodos,
	 * que son funciones que pueden aplicarse sobre el objeto y que pueden verse como los mensajes 
	 * que es posible enviar al objeto. Normalmente un objeto puede tener varia interfaces.
	 * 
	 * Como podemos ver en el ejemplo, al abstraer el concepto de transporte, sacamos propiedades
	 * como su kilometraje o su velocidad, abstrayendo el concepto para poder trabajarlo.
	 * 
	 */
}
